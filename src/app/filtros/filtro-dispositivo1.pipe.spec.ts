import { FiltroDispositivo1Pipe } from './filtro-dispositivo1.pipe';

describe('FiltroDispositivo1Pipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDispositivo1Pipe();
    expect(pipe).toBeTruthy();
  });
});
