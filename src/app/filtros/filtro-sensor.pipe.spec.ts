import { FiltroSensorPipe } from './filtro-sensor.pipe';

describe('FiltroSensorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSensorPipe();
    expect(pipe).toBeTruthy();
  });
});
